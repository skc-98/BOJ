#include<iostream>
using namespace std;

int main()
{
    cin.tie(NULL); cout.tie(NULL);
    long long n,m;
    cin >>n>>m;
    
    long long value=n*m;
    cout << value/2;
    return 0;
}